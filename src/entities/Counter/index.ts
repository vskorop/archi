import { counterReducer } from './model/slice/сounterSlice';
import { Counter } from './ui/Counter';
import type { CounterSchema } from '../Counter/model/types/counterSchema';

export {
    Counter,
    counterReducer,
    CounterSchema,
};
